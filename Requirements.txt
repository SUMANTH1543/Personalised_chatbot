# Writing the requirements to a 'requirements.txt' file
requirements = """
streamlit==1.25.0
transformers==4.31.0
torch==2.0.0
tokenizers==0.13.3
"""

with open("/mnt/data/requirements.txt", "w") as file:
    file.write(requirements)

"/mnt/data/requirements.txt"
